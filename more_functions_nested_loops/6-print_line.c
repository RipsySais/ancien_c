#include "main.h"
#include <stdio.h>
/**
 * print_line - une fonction qui trace une ligne droite
 * @n: n est le caractère a check
 * Return: Always
 */
void print_line(int n)
{
	int a;

	for (a = n; a < n; a++)
		_putchar('_');
	putchar('\n');

}
