#include "main.h"
#include <stdio.h>
/**
 * mul - une fonction qui multiplie deux entiers
 * @a : prémier number
 * @b : deuxième number
 * Return: retoune les entiers
 */
int int mul(int a, int b)

{
	return (a * b);
}
