#include "main.h"
#include <stdio.h>
/**
 * main - Verification des caractères majuscule
 * Return: Always 0
 */
int main(void)
{
	int _isupper(int c);

	{
		if (c >= 'A' && c <= 'z')
			return (1);

		else
			return (0);
	}
}	
